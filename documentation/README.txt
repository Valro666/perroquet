//connexion au proxy
git config --global http.proxy http://cache.uhp-nancy.fr:3128

//recuperer un fichier d'un ancienne revision
git show <numrev>:<file> > <newFile>

//affecter son email a git
git config --global user.email "your email"
git config --global user.name "your name"